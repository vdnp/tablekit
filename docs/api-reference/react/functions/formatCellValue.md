# Function: formatCellValue()

> **formatCellValue**(`value`): `ReactNode`

Default cell formatting for raw values: null-safe, locale-stable.

## Parameters

### value

`unknown`

## Returns

`ReactNode`
